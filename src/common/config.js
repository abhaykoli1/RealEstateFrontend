const config = {
  //:local host
  // API_URL: "http://localhost:8080",
  // -------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  // live host
  API_URL: "https://real-esate-oghre.ondigitalocean.app",
};

export default config;
